# Telegram bot
BOT_TOKEN=1234567890:ABC_your_token_here
# Channel destination: @username OR numeric id like -1001234567890 (bot must be admin in that channel)
CHANNEL_ID=@your_channel

# Admins who can moderate (comma separated Telegram user IDs) – optional if you use ADMIN_CHAT_ID
ADMINS=12345678,87654321
# Or point all review cards into one admin/group chat id (optional)
ADMIN_CHAT_ID=

# Timezone & DB
TIMEZONE=Europe/Kyiv
DB_PATH=news.db

# RSS sources (comma-separated). Можна залишити порожнім, якщо працюємо тільки з HTML.
SOURCES=

# YAML-файл із правилами для сайтів без RSS
SOURCES_YAML=sources.yaml

# Limit per collection run (anti-spam)
MAX_ITEMS_PER_RUN=10
